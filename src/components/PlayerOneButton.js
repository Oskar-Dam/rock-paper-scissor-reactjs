import * as React from 'react';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';

export default function PlayerOneButton() {
  return (
    <Button color="primary" variant="contained" style={{ position:"absolute", left: "19%", bottom: "40%" }}>Aukera hautatu</Button>

  );
}